# URL base do backend. Use para apontar o frontend ao backend (ex.: em produção ou outro host).
# Se não definida, o front usa "/api" e o proxy do Vite em dev encaminha para o backend.
# Exemplo: http://localhost:3001
# VITE_API_BASE_URL=http://localhost:3001
